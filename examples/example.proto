syntax = "proto3";

import "google/protobuf/descriptor.proto";

option php_namespace = "ROCRPC/xxx";

package example;

service ExampleService {
  rpc ping(Packet) returns (Packet) {}
}

message Packet {
  int32 id = 1;
}
